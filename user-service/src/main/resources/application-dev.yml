spring:
  config:
    activate:
      on-profile: dev

  datasource:
    url: jdbc:h2:mem:test

  docker:
    compose:
      enabled: false

  h2:
    console:
      enabled: true

  devtools:
    livereload:
      enabled: true
    add-properties: true